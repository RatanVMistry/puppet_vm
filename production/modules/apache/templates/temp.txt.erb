web_servers = []  

        File.foreach( '/tmp/test.txt' ) do |line|
                  array_line.push line
        end
        web_servers.each do |ipaddress|
          puts "server #{ipaddress}"
         end
        

